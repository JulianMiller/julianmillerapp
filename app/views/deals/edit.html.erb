<h1>Editing deal</h1>

<% form_for(@deal) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form',  :locals => { :f => f } %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @deal %> |
<%= link_to 'Back', deals_path %>